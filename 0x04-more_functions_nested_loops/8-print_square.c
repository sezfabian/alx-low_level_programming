#include<stdio.h>
#include "main.h"

/**
 * print_square - prints square using '#' on terminal
 *
 * @size : size of square
 *
 * Return: 0 always - sucess.
 */
void print_square(int size)
{
int count;
int a;
if (n > 0)
{
for (a = 1; a <= size; ++a)
{
for (count = 1; count <= size; ++count)
{
_putchar('#');
}
_putchar('\n');
}
}
else
{
_putchar('\n');
}
}
