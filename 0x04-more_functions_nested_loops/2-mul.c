include<stdio.h>

/**
 * mul - finds multiple of two integers
 *
 * @a : integer one
 * @b : integer two
 *
 * Return: 1 for digit, 0 otherwise.
 */

int mul(int a, int b)
{
int m;
m = a * b;

return (m);
}
